<% include ../partials/header %>

    <div class="container">
        <div class="row">
            <div class="col-md-3">
                <p class="lead">YelpCamp</p>
                <div class="list-group">
                    <li class="list-group-item active">Info 1</li>
                    <li class="list-group-item">Info 2</li>
                    <li class="list-group-item">Info 3</li>
                </div>
            </div>
            <div class="col-md-9">
                <div class="thumbnail">
                   <img src="<%= campground.image %>" class="image-responsive"> 
                    <div class="caption-full">
                        <h5 class="pull-right">$10.00/night</h5>
                        <h3><a href="/campgrounds/<%= campground._id %>"><%= campground.name %></a></h3>
                        <p><%= campground.description %></p>
                        <p>Posted by: <%= campground.author.username %></p>
                    </div>
                </div>
                <div class="well">
                    <div class="text-right">
                        <a href="/campgrounds/<%= campground._id %>/comments/new" class="btn btn-success">Add new comment</a>
                    </div>
                    <br><br>
                    <% campground.comments.forEach(function(comment) { %>
                        <div class="row">
                            <div class="col-md-12">
                                <strong><%= comment.author.username %></strong>
                                <span class="pull-right">5 days ago</span>
                                <p><%= comment.text %></p>
                            </div>
                        </div>
                    <% }); %>
                </div>
            </div>
        </div>
    </div>

<% include ../partials/footer %>